# LeetCode 183：从题面到代码的思维与避坑指南

本篇配合 [183 题索引](leetcode_hot100_interview150.md) 使用。它不把题目当作需要背诵的模板，而是训练一条稳定的链路：**识别题目约束 → 选择要维护的事实（不变量）→ 让每一步代码只破坏并恢复一个事实 → 用边界例子反证实现**。

## 先建立统一的解题流程

### 1. 不要先问“这是什么模板”

先把题意改写为三句话：

1. **对象**：数组、字符串、链表、树、图，还是状态空间？
2. **动作**：删/合并/寻找/计数/最优化/枚举，允许不允许修改输入？
3. **保证**：是否有序、元素是否非负、是否存在解、是否有环、规模上限是什么？

例如“最短满足条件的子数组”，关键不是“滑动窗口”四个字，而是：元素非负使得窗口向右扩张时和不减，向左收缩时和不增，因此一个双端窗口能单调地淘汰不可能的左端点。若允许负数，这个单调性消失，不能照搬窗口。

### 2. 先写不变量，再写循环

每个循环至少能回答一个问题：

- `left..right` 当前代表什么？
- 哈希表/栈/堆中保存的元素为何仍然有效？
- 递归函数返回值究竟表达哪件事？
- 某个答案一旦更新，为什么以后不会被推翻？

写不出一句话不变量时，先暂停编码。大多数“差一个边界”的 bug，本质是变量没有明确语义。

### 3. 用最小反例验收

每题至少在脑中走一遍：空输入（若允许）、单元素、全相同、完全无解、答案位于开头/结尾、重复值、极值、只有一层/一条边。链表和树再额外检查 `None`；图再检查孤立点与环。

## 哈希、计数与前缀和

### 如何从题面想到它

看到“是否出现过”“配对”“频次”“相同模式”“连续和为某值”，先问：我是否需要快速回答“此前是否存在某个状态”？如果是，哈希表通常比排序更直接。

- 两数之和的事实是“已扫过数的补数是否存在”。
- 异位词的事实是“字符频次向量是否相同”。
- 前缀和题的事实是：`prefix[j] - prefix[i] = k` 等价于“此前出现过 `prefix[j] - k`”。

### 编码不变量与坑

- **先查还是先写**取决于是否允许同一元素复用。两数之和应先查补数再写当前数；前缀和计数应先计入已有前缀，再写当前前缀。
- 前缀和频次从 `{0: 1}` 开始，否则漏掉从下标 0 开始的答案。
- 将可变对象当字典键前，转成元组；分组异位词时不要复用会被后续修改的列表。
- Boyer–Moore 投票只在“多数元素必存在”时直接成立；缺少该保证要二次验证。

## 双指针、原地数组与区间

### 先找“一个指针移动后不会后悔”的依据

有序数组、两端的物理约束、或排序后的区间，通常提供淘汰方向。盛水容器移动短板，因为保留短板不可能提高面积上界；三数之和移动左右指针，因为数组有序；合并区间按左端点排序，因为未来只可能影响当前合并段的右边界。

### 编码不变量与坑

- 快慢指针中，`slow` 的语义应是“下一个可写位置”，不要同时把它当成“最后一个有效位置”。
- 原地删除题返回的是**有效长度**；LeetCode 不要求清掉尾部垃圾值。
- 三数之和：固定下标也要跳重，左右指针命中后同样跳重；跳重前后不要越界。
- 区间端点是闭区间还是半开区间必须从题目确认。`start <= last_end` 与 `start < last_end` 的差异会影响“相邻是否合并”。
- 矩阵右上角搜索只适用于行、列均递增；不要和“整体有序矩阵”的一维二分混用。

## 数组与矩阵：先定义坐标与原地状态

数组/矩阵模拟题往往没有高深算法，难在状态同时变化。先写出坐标含义和更新顺序：螺旋遍历维护四条尚未访问的边界；旋转矩阵可拆成“转置 + 每行反转”；矩阵置零将首行首列借作标记时，要先独立保存首行/首列是否本来为零。

涉及原地修改时，给状态编码：生命游戏可用不同数值同时表示旧状态和新状态，最后统一归一化，避免新状态提前影响邻居统计。字符串模拟同理：把读指针、写指针、方向、进位分开命名；每次循环只推进一个清晰的状态机步骤。

## 滑动窗口

### 适用条件不是“子串/子数组”

窗口成立的核心是：右扩与左缩对约束有可预测的单调影响。典型信号是“最长/最短连续片段，满足频次、种类数、和等约束”。

写代码前定义：

- 窗口何时**非法**？例如重复字符、缺少目标字符、和不足。
- 何时更新答案？最长通常在合法后更新；最短通常在合法时不断收缩再更新。
- 窗口统计的含义是“窗口内频次”，不是“已遍历频次”。

### 编码不变量与坑

- 右指针每轮只新增一个元素；左指针每轮只删除一个元素。不要一次性重建计数表。
- 最小覆盖子串中，`required` 应表示尚未满足的字符总数；只有加入仍然需要的字符才减一，移出后变为正需求才加一。
- 固定长度窗口要先移出过期元素，再判断/记录；可变长度窗口要在 `while 非法` 中持续收缩。
- 单调队列存**下标**而非值：既能判断过期，又能处理重复值。

## 栈、单调栈与表达式

### 把“未完成的关系”压栈

栈适合处理“当前元素要等未来元素来配对/结算”的题：括号等右括号，温度等更高温，柱子等右侧更低边界，表达式等操作数。

单调栈的思路是反过来问：当新元素到来时，谁的“下一个更大/更小元素”终于确定了？被弹出的元素正是答案可以结算的对象。

### 编码不变量与坑

- 括号题映射为“右括号 → 对应左括号”，出栈前检查栈非空。
- 柱状图在末尾加高度 `0` 的哨兵，强制结算仍留在栈中的柱子；宽度是 `i - stack[-1] - 1`。
- 计算器优先明确是否含乘除、是否含一元负号、是否含括号；不同题目的语法不同，不可直接复用。
- Python 整数除法向零截断应写 `int(a / b)`，`//` 对负数是向下取整。

## 链表：先画“边”，再改“边”

链表题的难点不是算法，而是指针改写顺序。先画出局部三到四个节点，并用一句话命名每根边：`prev` 是已处理段尾，`cur` 是待处理头，`next_node` 是尚未丢失的后继。

### 常见推导

- 需要删/插头节点：建立 `dummy`，让“头节点也是普通节点的后继”。
- 需要倒数第 N 个：两个指针先拉开 `n` 步；`dummy` 能统一删除头节点。
- 需要找中点/判环：快慢指针；奇偶长度要先决定慢指针停在左中还是右中。
- 需要反转：每次只做三步：保存 `next_node`、反向连 `cur.next`、推进 `prev/cur`。
- 需要随机访问最近使用项：哈希表负责 O(1) 定位，双向链表负责 O(1) 删除与移动。

### 最容易漏的情况

- 空链表、单节点、两节点、删除头节点、`n == length`。
- 分区/合并后，尾节点必须显式 `next = None`，否则会遗留旧链造成环。
- K 个一组反转时，先确认组长度够 `k`，再改任何指针。
- 回文链表若允许修改输入可反转后半；若题目要求保留原链，要把它复原。
- Floyd 判环的第二阶段：相遇后一个指针回头，两者同速走才在入口相遇；不要继续用快慢速度。

## 二分查找与“答案二分”

二分不是“数组有序”这么简单，而是寻找某个单调谓词从假变真或从真变假的边界。

### 编码前先写谓词

- 搜索插入位置：`nums[mid] >= target` 是否成立。
- 最小可行速度/容量：`can_finish(x)` 随 `x` 增大单调变真。
- 峰值：比较 `nums[mid]` 和 `nums[mid + 1]`，上坡侧必存在峰。

### 编码不变量与坑

- 明确使用闭区间 `[lo, hi]` 还是半开区间 `[lo, hi)`，全题保持一致。
- 中点写成 `lo + (hi - lo) // 2`；更新边界时保证区间变小，避免 `lo = mid` 死循环。
- 旋转数组先判断哪半有序，再判断 target 是否落在该半；重复元素会改变这一逻辑，不能直接套用无重复版本。
- 二分答案的 `hi` 必须是可行上界，`lo` 必须含有合法下界；写两个极端例子验证。

## 树：递归函数返回“给父节点的贡献”

面对树，先选遍历顺序，不要急着写递归。父节点要用子节点的结果做什么，决定用前序、后序还是层序。

- 构造/序列化：前序更自然，因为先确定根。
- 高度、直径、最大路径和：后序，因为父节点依赖孩子贡献。
- BST 第 K 小：中序，因为值按升序出现。
- 每层统计、右视图、平均值：BFS，因为答案天然按层定义。

### 关键不变量与坑

- 递归基必须处理 `None`；返回高度通常定义空树高度为 0。
- 最大路径和：向父节点最多返回一条向下路径，左右两支只能在当前节点拼成全局答案。
- LCA：左右子树各返回一个非空结果时当前节点才是答案；若当前节点就是目标，直接返回它。
- BST 校验不能只比较父子，应传递全局上下界。
- 构造树时用哈希表定位中序下标，递归参数必须是明确的左右闭/开边界，避免切片造成 O(n²)。

## 图、网格 DFS/BFS 与拓扑排序

### 先决定“节点、边、访问状态”

网格的格子是节点，四/八方向是边；课程依赖是有向边；单词变换是隐式图。明确邻接关系后，算法选择自然出现：连通块用 DFS/BFS，最少步数用 BFS，有向依赖用拓扑排序。

### 编码不变量与坑

- 入队/入栈时就标记访问，避免同一节点被多条边重复加入。
- 多源 BFS（腐烂橘子）先把所有源入队；队列的一层代表同一时间步。
- 网格题先检查边界，再访问；原地改写网格要确认允许修改输入。
- 拓扑排序处理完的节点数小于 `n` 即有环；建图方向必须与“前置课 → 后续课”一致。
- DFS 判环用三色状态：未访问、访问中、已完成；遇到“访问中”才是回边。
- 并查集仅适合无向连通性；有向可达/最短路不是它的职责。

## 回溯、Trie 与穷举剪枝

回溯的本质是枚举决策树，不是“递归万能”。先写清楚每层决定什么、终止条件是什么、哪些前缀已经不可能成功。

### 编码不变量与坑

- `path.append(...)` 后必须在返回前 `path.pop()`；加入答案要用 `path[:]`，不能直接保存同一个列表引用。
- 组合类题用 `start` 防止排列重复；全排列用 `used` 或交换位置。
- 去重依赖排序时，同层跳过相同值，但不要跨层错误跳过。
- N 皇后用列、主对角线 `r-c`、副对角线 `r+c` 三个集合；撤销顺序与加入对应。
- Trie 中 `is_end` 与“前缀存在”是两件事；单词搜索 II 找到单词后可清空 `is_end` 去重，但不要删除仍可能服务其他单词的路径。

## 堆、优先队列与多路归并

堆只应保存“下一步可能成为答案的候选”，而不是所有中间状态。

- K 路合并：每条链/数组当前头是候选，弹出后只补入它的后继。
- Top K：维护大小为 `k` 的小根堆，堆顶是当前第 k 大的门槛。
- 双堆中位数：左堆存较小半边，右堆存较大半边，始终保证大小差不超过 1。
- 项目/区间题：一类堆按“何时可用”推进，另一类堆按“当前最优”选择。

### Python 坑

- `heapq` 只有小根堆，最大堆用负数，或在元组首项放负优先级。
- 元组优先级相同且后续对象不可比较时，加入自增计数器作为第二项。
- 不要试图在堆内原地更新元素；通常推入新状态，弹出时跳过过期状态。

## 贪心与 Kadane

贪心不是“每次选看起来最好”，而是要找到**交换论证**：若最优解不做当前选择，能否把它替换成当前选择且不变差？

- 跳跃游戏维护最远可达位置；一旦当前位置不可达，后面都无意义。
- 分发糖果分别满足左、右邻居约束，最终取两次需求的最大值。
- 划分字母区间维护当前段内字符最远末次出现；边界到达后切割不会影响未来。
- Kadane 保留“以当前位置结尾的最佳和”：若前缀和为负，带着它只会拖累未来，直接重启。

编码时特别确认贪心是否要求排序，以及“相等”应不应该更新；这些决定经常藏在题目的严格/非严格不等号中。

## 动态规划：状态是信息最少但足够的历史

写 DP 前先拒绝写 `dp` 数组。先回答：为了做下一步选择，过去的哪些信息不可丢失？这就是状态。

### 常见状态来源

- 线性位置：`dp[i]` 表示到第 i 个位置的最佳/可达性（爬楼梯、打家劫舍、零钱）。
- 两个前缀：`dp[i][j]` 表示两个字符串/网格前缀的答案（编辑距离、LCS、路径）。
- 交易状态：天数 + 持仓 + 已完成交易数（股票 III/IV）。
- 区间：`dp[l][r]` 表示一段区间的答案，枚举最后一次决策。

### 编码不变量与坑

- 初始值是转移的一部分：不可达用 `inf/-inf`，不要误用 0。
- 0/1 背包容量必须倒序遍历；完全背包通常正序。方向错误会把一个物品重复使用。
- 二维 DP 的第一行/第一列常表示空前缀，先初始化再转移。
- 空间压缩前确认当前状态只依赖“上一行/上一列”；覆盖方向必须与依赖方向一致。
- 乘积最大子数组要同时保留最大和最小，因为负数会交换它们的作用。

## 位运算、数学与字符串模拟

这类题的关键是先消除语言细节：Python 整数没有固定 32 位溢出，题目若要求 32 位逻辑，要主动掩码或控制循环次数。

- XOR：相同数抵消，适合成对出现问题。
- `x & (x - 1)`：删除最低位的 1，适合统计 set bits。
- 区间按位与：不断右移直到两端公共前缀相同。
- 快速幂：把指数按二进制拆分；负指数先取倒数。
- 字符串模拟：先定义读写指针、进位、方向等状态，避免边遍历边修改原字符串导致下标混乱。

## 提交前的最后一分钟

1. 输入是否被允许修改？若不允许，是否需要复制？
2. 所有变量是否有单一语义？尤其是 `left/right/slow/prev`。
3. 循环是否保证推进？递归是否保证缩小问题？
4. 空、单元素、重复、无解、边界答案是否覆盖？
5. 复杂度是否真的匹配约束？嵌套循环是否被单调指针均摊掉？

真正稳定的刷题能力来自：遇到新题时能说清“我准备维护什么事实、为什么丢弃某些状态安全、以及每一步会破坏什么边界”。答案代码只是这套推理的最后一页。
